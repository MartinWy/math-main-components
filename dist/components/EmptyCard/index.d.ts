import React from 'react';
export declare function EmptyCard({ title, subtitle, iconName, button, onClick }: {
    title?: string;
    subtitle?: string;
    iconName?: string;
    button?: string;
    onClick?: () => void;
}): React.JSX.Element;
